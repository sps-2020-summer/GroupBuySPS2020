/**
 * Backend method
 * @param firebase firebase object
 */
export const dummyMethod = (firebase: firebase.firestore.Firestore) => {
  // Need to pass in the fire base object from FE side if im not mistaken
  console.log("pls expose this so i can fetch");
};
